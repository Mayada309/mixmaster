import styled from 'styled-components';

const Wrapper = styled.div`
  p {
    max-width: 1000px;
    line-height: 2;
    color: var(--grey-500);
    margin-top: 2rem;
  }
`;

export default Wrapper;
